//Declare a variable named multiply with a function expression
const square = function (number) {
    return number * number;
};

// Call the function with a number and log the square
console.log(square(8)); // 64